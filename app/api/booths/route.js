import { NextResponse } from "next/server";
import { query } from "@/app/_lib/db";

export async function GET() {
  //   const results = await query("SELECT * FROM Booths");
  //   return NextResponse.json(results);
}
